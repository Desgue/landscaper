/** Size of each terrain tile texture in pixels (matches 100cm grid cells). */
export const TILE_SIZE = 100

/** Vertical extrusion scale factor: sin(30deg) for implied camera tilt. */
export const EXTRUSION_SCALE = 0.5

/** Camera tilt angle in degrees (documentation only). */
export const EXTRUSION_ANGLE_DEG = 30

/** Maximum atlas page dimension in pixels. */
export const MAX_ATLAS_SIZE = 2048

/** Fallback color for missing/unknown textures. */
export const FALLBACK_COLOR = '#FF00FF'
